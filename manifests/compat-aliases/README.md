# Compatibility Aliases

Compatibility aliases map legacy labels, service ids, and route names to
canonical Agent OS ids.

The alias layer is transitional. New runtime code and new manifests should use
canonical ids. Compatibility aliases are allowed at external boundaries,
launchers, migration docs, and legacy adapter shims.
