# Communications

Runtime communication policy belongs here.

Agent OS tracks communication boundaries without forcing every organ to use the
same protocol. Connection manifests should describe data shape, authority,
owner, health, and future control handles such as pause, resume, reset,
shutdown, and stats.

